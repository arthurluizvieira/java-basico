public class notas{

    String aluno;

    double calcularNotas(double nota1, double nota2, double nota3) {

        double notas = (nota1 + nota2 + nota3);
        
        return notas / 3;
         

    }
}